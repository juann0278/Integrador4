package org.example.gateway.security.jwt;

import org.springframework.security.core.Authentication;

public class TokenProvider {
    public Object createToken(Authentication authentication) {
    }
}
