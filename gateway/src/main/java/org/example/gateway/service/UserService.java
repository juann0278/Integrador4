package org.example.gateway.service;

import jakarta.validation.Valid;
import org.example.gateway.service.dto.user.UserDTO;

public class UserService {
    public Object saveUser(@Valid UserDTO userDTO) {
    }
}
